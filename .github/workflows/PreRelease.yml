name: PreRelease Bump Version

on:
  workflow_dispatch:

jobs:
  bump_version:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Bump version
      run: node PreRelease_node.js

    - name: Commit and push changes
      run: |
        git config --global user.name 'tcfshcrw'
        git config --global user.email 'crw010284@gmail.com'
        git add JSON_test/dev/Bridge/Version_devkit.json
        git commit -m "PreRelease Bump version"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
